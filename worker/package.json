{
  "name": "ai-gftd-open-isic",
  "version": "0.1.0",
  "description": "Open-source UN ISIC Rev.4 industrial classification as a DID-addressed taxonomy — implemented class-by-class on CF Workers",
  "private": false,
  "license": "Apache-2.0",
  "main": "src/app.ts",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250415.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
